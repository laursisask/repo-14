{
    "id": "ssh",
    "name": "SSH",
    "description": "Sets up SSH into the Dev Environment",
    "version": "1.1.2",
    "options": {
        "enabled": {
            "type": "boolean",
            "default": true,
            "description": "Enable OpenSSH"
        }
    }
}
